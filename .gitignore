# Terraform local state & cache
.terraform/          # local provider & module cache
*.tfstate            # local state file
*.tfstate.*          # backup state files

# Sensitive variable files
*.tfvars             # local variables (do not commit secrets)
*.tfvars.json

# Crash logs
crash.log

# Lock file (optional)
# Keep this if you want reproducible builds across machines
.terraform.lock.hcl